       /*
 * THIS IS A GENERATED CLASS. THIS CLASS SHOULD NOT BE EDITED.
 */
package com.rsaame.pas.pojo.mapper;

import com.mindtree.ruc.cmn.beanmap.BaseBeanToBeanMapper;
import com.mindtree.ruc.cmn.log.Logger;
import com.mindtree.ruc.cmn.utils.Utils;

/**
 * Mapper class for:<ol>
 * <li>com.rsaame.pas.dao.model.TTrnWctplPersonQuo</li>
 * <li>com.rsaame.pas.vo.svc.TTrnWctplPersonQuoHolder</li>
 * </ol>
 * Get an instance of this class by making a call to <code>BeanMapperFactory.getMapperInstance( TTrnWctplPersonHolderToTTrnWctplPersonQuoMapperReverse.class )</code>.
 */
public class TTrnWctplPersonHolderToTTrnWctplPersonQuoMapperReverse extends BaseBeanToBeanMapper<com.rsaame.pas.dao.model.TTrnWctplPersonQuo, com.rsaame.pas.vo.svc.TTrnWctplPersonQuoHolder>{
	private final Logger log = Logger.getLogger( this.getClass() );	

	public TTrnWctplPersonHolderToTTrnWctplPersonQuoMapperReverse(){
		super();
	}

	public TTrnWctplPersonHolderToTTrnWctplPersonQuoMapperReverse( com.rsaame.pas.dao.model.TTrnWctplPersonQuo src, com.rsaame.pas.vo.svc.TTrnWctplPersonQuoHolder dest ){
		super( src, dest );
	}

	/**
	 * This method maps the contents of the source bean to a destination bean.
	 * 
	 * @param src
	 * @param dest
	 */
	@Override
	public com.rsaame.pas.vo.svc.TTrnWctplPersonQuoHolder mapBean(){
		
		/* Simply return if the source is null. */
		if( Utils.isEmpty( src ) ){
			return null;
		}
		
		/* If the destination is null, instantiate it. */
		if( Utils.isEmpty( dest ) ){
			dest = (com.rsaame.pas.vo.svc.TTrnWctplPersonQuoHolder) Utils.newInstance( "com.rsaame.pas.vo.svc.TTrnWctplPersonQuoHolder" );
		}
		
		/* Cast the destination bean to a bean of type of BeanA */	
		com.rsaame.pas.dao.model.TTrnWctplPersonQuo beanA = src;
			
		/* Cast the destination bean to a bean of type of BeanB */
		com.rsaame.pas.vo.svc.TTrnWctplPersonQuoHolder beanB = dest;
			
  		/* Initialize any deepVO inside the destination bean. This is to avoid null pointers at runtime whenever a deep field is being mapped in the destination bean. */
		beanB = initializeDeepVO(beanA, beanB);
		
		/* A common variable that will be used to store the number of items in the case of multi-mappings. */
		int noOfItems = 0;
		
		/* Mapping: "id.wprId" -> "wprId" */
		if(  !Utils.isEmpty( beanA.getId() ) && !Utils.isEmpty( beanA.getId().getWprId() )  ){
 			beanB.setWprId( beanA.getId().getWprId() ); 
 		}

 		/* Mapping: "id.wprValidityStartDate" -> "wprValidityStartDate" */
		if(  !Utils.isEmpty( beanA.getId() ) && !Utils.isEmpty( beanA.getId().getWprValidityStartDate() )  ){
 			beanB.setWprValidityStartDate( beanA.getId().getWprValidityStartDate() ); 
 		}

 		/* Mapping: "wprPolicyId" -> "wprPolicyId" */
		if(  !Utils.isEmpty( beanA.getWprPolicyId() )  ){
 			beanB.setWprPolicyId( beanA.getWprPolicyId() ); 
 		}

 		/* Mapping: "wprBasicRiskId" -> "wprBasicRiskId" */
		if(  !Utils.isEmpty( beanA.getWprBasicRiskId() )  ){
 			beanB.setWprBasicRiskId( beanA.getWprBasicRiskId() ); 
 		}

 		/* Mapping: "wprRskCode" -> "wprRskCode" */
		if(  !Utils.isEmpty( beanA.getWprRskCode() )  ){
 			beanB.setWprRskCode( beanA.getWprRskCode() ); 
 		}

 		/* Mapping: "wprRtCode" -> "wprRtCode" */
		if(  !Utils.isEmpty( beanA.getWprRtCode() )  ){
 			beanB.setWprRtCode( beanA.getWprRtCode() ); 
 		}

 		/* Mapping: "wprRcCode" -> "wprRcCode" */
		if(  !Utils.isEmpty( beanA.getWprRcCode() )  ){
 			beanB.setWprRcCode( beanA.getWprRcCode() ); 
 		}

 		/* Mapping: "wprAName" -> "wprAName" */
		if(  !Utils.isEmpty( beanA.getWprAName() )  ){
 			beanB.setWprAName( beanA.getWprAName() ); 
 		}

 		/* Mapping: "wprEName" -> "wprEName" */
		if(  !Utils.isEmpty( beanA.getWprEName() )  ){
 			beanB.setWprEName( beanA.getWprEName() ); 
 		}

 		/* Mapping: "wprAAddress1" -> "wprAAddress1" */
		if(  !Utils.isEmpty( beanA.getWprAAddress1() )  ){
 			beanB.setWprAAddress1( beanA.getWprAAddress1() ); 
 		}

 		/* Mapping: "wprZip" -> "wprZip" */
		if(  !Utils.isEmpty( beanA.getWprZip() )  ){
 			beanB.setWprZip( beanA.getWprZip() ); 
 		}

 		/* Mapping: "wprTelephone" -> "wprTelephone" */
		if(  !Utils.isEmpty( beanA.getWprTelephone() )  ){
 			beanB.setWprTelephone( beanA.getWprTelephone() ); 
 		}

 		/* Mapping: "wprEmail" -> "wprEmail" */
		if(  !Utils.isEmpty( beanA.getWprEmail() )  ){
 			beanB.setWprEmail( beanA.getWprEmail() ); 
 		}

 		/* Mapping: "wprAge" -> "wprAge" */
		if(  !Utils.isEmpty( beanA.getWprAge() )  ){
 			beanB.setWprAge( beanA.getWprAge() ); 
 		}

 		/* Mapping: "wprSumInsured" -> "wprSumInsured" */
		if(  !Utils.isEmpty( beanA.getWprSumInsured() )  ){
 			beanB.setWprSumInsured( beanA.getWprSumInsured() ); 
 		}

 		/* Mapping: "wprStatus" -> "wprStatus" */
		if(  !Utils.isEmpty( beanA.getWprStatus() )  ){
 			beanB.setWprStatus( beanA.getWprStatus() ); 
 		}

 		/* Mapping: "wprEmploymentType" -> "wprEmploymentType" */
		if(  !Utils.isEmpty( beanA.getWprEmploymentType() )  ){
 			beanB.setWprEmploymentType( beanA.getWprEmploymentType() ); 
 		}

 		/* Mapping: "wprEmployerEName" -> "wprEmployerEName" */
		if(  !Utils.isEmpty( beanA.getWprEmployerEName() )  ){
 			beanB.setWprEmployerEName( beanA.getWprEmployerEName() ); 
 		}

 		/* Mapping: "wprEmployerAName" -> "wprEmployerAName" */
		if(  !Utils.isEmpty( beanA.getWprEmployerAName() )  ){
 			beanB.setWprEmployerAName( beanA.getWprEmployerAName() ); 
 		}

 		/* Mapping: "wprValidityExpiryDate" -> "wprValidityExpiryDate" */
		if(  !Utils.isEmpty( beanA.getWprValidityExpiryDate() )  ){
 			beanB.setWprValidityExpiryDate( beanA.getWprValidityExpiryDate() ); 
 		}

 		/* Mapping: "wprEndtId" -> "wprEndtId" */
		if(  !Utils.isEmpty( beanA.getWprEndtId() )  ){
 			beanB.setWprEndtId( beanA.getWprEndtId() ); 
 		}

 		/* Mapping: "wprRiRskCode" -> "wprRiRskCode" */
		if(  !Utils.isEmpty( beanA.getWprRiRskCode() )  ){
 			beanB.setWprRiRskCode( beanA.getWprRiRskCode() ); 
 		}

 		/* Mapping: "wprBasicRskCode" -> "wprBasicRskCode" */
		if(  !Utils.isEmpty( beanA.getWprBasicRskCode() )  ){
 			beanB.setWprBasicRskCode( beanA.getWprBasicRskCode() ); 
 		}

 		/* Mapping: "wprSalary" -> "wprSalary" */
		if(  !Utils.isEmpty( beanA.getWprSalary() )  ){
 			beanB.setWprSalary( beanA.getWprSalary() ); 
 		}

 		/* Mapping: "wprPreparedBy" -> "wprPreparedBy" */
		if(  !Utils.isEmpty( beanA.getWprPreparedBy() )  ){
 			beanB.setWprPreparedBy( beanA.getWprPreparedBy() ); 
 		}

 		/* Mapping: "wprPreparedDt" -> "wprPreparedDt" */
		if(  !Utils.isEmpty( beanA.getWprPreparedDt() )  ){
 			beanB.setWprPreparedDt( beanA.getWprPreparedDt() ); 
 		}

 		/* Mapping: "wprModifiedBy" -> "wprModifiedBy" */
		if(  !Utils.isEmpty( beanA.getWprModifiedBy() )  ){
 			beanB.setWprModifiedBy( beanA.getWprModifiedBy() ); 
 		}

 		/* Mapping: "wprModifiedDt" -> "wprModifiedDt" */
		if(  !Utils.isEmpty( beanA.getWprModifiedDt() )  ){
 			beanB.setWprModifiedDt( beanA.getWprModifiedDt() ); 
 		}

 		/* Mapping: "wprStartDate" -> "wprStartDate" */
		if(  !Utils.isEmpty( beanA.getWprStartDate() )  ){
 			beanB.setWprStartDate( beanA.getWprStartDate() ); 
 		}

 		/* Mapping: "wprEndDate" -> "wprEndDate" */
		if(  !Utils.isEmpty( beanA.getWprEndDate() )  ){
 			beanB.setWprEndDate( beanA.getWprEndDate() ); 
 		}

 		/* Mapping: "wprHazard" -> "wprHazard" */
		if(  !Utils.isEmpty( beanA.getWprHazard() )  ){
 			beanB.setWprHazard( beanA.getWprHazard() ); 
 		}

 		/* Mapping: "wprTradeGroup" -> "wprTradeGroup" */
		if(  !Utils.isEmpty( beanA.getWprTradeGroup() )  ){
 			beanB.setWprTradeGroup( beanA.getWprTradeGroup() ); 
 		}

 		/* Mapping: "wprBldId" -> "wprBldId" */
		if(  !Utils.isEmpty( beanA.getWprBldId() )  ){
 			beanB.setWprBldId( beanA.getWprBldId() ); 
 		}

 		/* Mapping: "wprTerCode" -> "wprTerCode" */
		if(  !Utils.isEmpty( beanA.getWprTerCode() )  ){
 			beanB.setWprTerCode( beanA.getWprTerCode() ); 
 		}

 		/* Mapping: "wprJurCode" -> "wprJurCode" */
		if(  !Utils.isEmpty( beanA.getWprJurCode() )  ){
 			beanB.setWprJurCode( beanA.getWprJurCode() ); 
 		}

 		/* Mapping: "wprFreeZone" -> "wprFreeZone" */
		if(  !Utils.isEmpty( beanA.getWprFreeZone() )  ){
 			beanB.setWprFreeZone( beanA.getWprFreeZone() ); 
 		}

 		/* Mapping: "wprJurDesc" -> "wprJurDesc" */
		if(  !Utils.isEmpty( beanA.getWprJurDesc() )  ){
 			beanB.setWprJurDesc( beanA.getWprJurDesc() ); 
 		}

 		/* Mapping: "wprEmpLiabLmt" -> "wprEmpLiabLmt" */
		if(  !Utils.isEmpty( beanA.getWprEmpLiabLmt() )  ){
 			beanB.setWprEmpLiabLmt( beanA.getWprEmpLiabLmt() ); 
 		}

   
		return dest;
	}	  

	/**
	 * This method initialises all the complex objects and collection instances we need for mapping.
	 */
	private static com.rsaame.pas.vo.svc.TTrnWctplPersonQuoHolder initializeDeepVO( com.rsaame.pas.dao.model.TTrnWctplPersonQuo beanA, com.rsaame.pas.vo.svc.TTrnWctplPersonQuoHolder beanB ){
                                                                             		return beanB;
	}
}
