       /*
 * THIS IS A GENERATED CLASS. THIS CLASS SHOULD NOT BE EDITED.
 */
package com.rsaame.pas.pojo.mapper;

import com.mindtree.ruc.cmn.beanmap.BaseBeanToBeanMapper;
import com.mindtree.ruc.cmn.beanmap.BeanMapperFactory;
import com.mindtree.ruc.cmn.beanmap.ConverterFactory;
import com.mindtree.ruc.cmn.log.Logger;
import com.mindtree.ruc.cmn.utils.BeanUtils;
import com.mindtree.ruc.cmn.utils.CopyUtils;
import com.mindtree.ruc.cmn.utils.Utils;

/**
 * Mapper class for:<ol>
 * <li>com.rsaame.pas.vo.app.PremiumSummaryVO</li>
 * <li>com.rsaame.pas.dao.model.VTrnPasPremiumSummary</li>
 * </ol>
 * Get an instance of this class by making a call to <code>BeanMapperFactory.getMapperInstance( PremiumSummaryVOToVTrnPasPremium.class )</code>.
 */
public class PremiumSummaryVOToVTrnPasPremium extends BaseBeanToBeanMapper<com.rsaame.pas.vo.app.PremiumSummaryVO, com.rsaame.pas.dao.model.VTrnPasPremiumSummary>{
	private final Logger log = Logger.getLogger( this.getClass() );	

	public PremiumSummaryVOToVTrnPasPremium(){
		super();
	}

	public PremiumSummaryVOToVTrnPasPremium( com.rsaame.pas.vo.app.PremiumSummaryVO src, com.rsaame.pas.dao.model.VTrnPasPremiumSummary dest ){
		super( src, dest );
	}

	/**
	 * This method maps the contents of the source bean to a destination bean.
	 * 
	 * @param src
	 * @param dest
	 */
	@Override
	public com.rsaame.pas.dao.model.VTrnPasPremiumSummary mapBean(){
		
		/* Simply return if the source is null. */
		if( Utils.isEmpty( src ) ){
			return null;
		}
		
		/* If the destination is null, instantiate it. */
		if( Utils.isEmpty( dest ) ){
			dest = (com.rsaame.pas.dao.model.VTrnPasPremiumSummary) Utils.newInstance( "com.rsaame.pas.dao.model.VTrnPasPremiumSummary" );
		}
		
		/* Cast the destination bean to a bean of type of BeanA */	
		com.rsaame.pas.vo.app.PremiumSummaryVO beanA = src;
			
		/* Cast the destination bean to a bean of type of BeanB */
		com.rsaame.pas.dao.model.VTrnPasPremiumSummary beanB = dest;
			
  		/* Initialize any deepVO inside the destination bean. This is to avoid null pointers at runtime whenever a deep field is being mapped in the destination bean. */
		beanB = initializeDeepVO(beanA, beanB);
		
		/* A common variable that will be used to store the number of items in the case of multi-mappings. */
		int noOfItems = 0;
		
		/* Mapping: "linkingId" -> "id.linkingId" */
		if(  !Utils.isEmpty( beanA.getLinkingId() )  ){
 			beanB.getId().setLinkingId( beanA.getLinkingId() ); 
 		}

 		/* Mapping: "policyId" -> "id.policyId" */
		if(  !Utils.isEmpty( beanA.getPolicyId() )  ){
 			beanB.getId().setPolicyId( beanA.getPolicyId() ); 
 		}

 		/* Mapping: "endtId" -> "id.endtId" */
		if(  !Utils.isEmpty( beanA.getEndtId() )  ){
 			beanB.getId().setEndtId( beanA.getEndtId() ); 
 		}

 		/* Mapping: "class_" -> "id.class_" */
		if(  !Utils.isEmpty( beanA.getClass_() )  ){
 			beanB.getId().setClass_( beanA.getClass_() ); 
 		}

 		/* Mapping: "secId" -> "id.secId" */
		if(  !Utils.isEmpty( beanA.getSecId() )  ){
 			beanB.getId().setSecId( beanA.getSecId() ); 
 		}

 		/* Mapping: "secName" -> "id.secName" */
		if(  !Utils.isEmpty( beanA.getSecName() )  ){
 			beanB.getId().setSecName( beanA.getSecName() ); 
 		}

 		/* Mapping: "locationId" -> "id.locationId" */
		if(  !Utils.isEmpty( beanA.getLocationId() )  ){
 			beanB.getId().setLocationId( beanA.getLocationId() ); 
 		}

 		/* Mapping: "locationName" -> "id.locationName" */
		if(  !Utils.isEmpty( beanA.getLocationName() )  ){
 			beanB.getId().setLocationName( beanA.getLocationName() ); 
 		}

 		/* Mapping: "coverId" -> "id.coverId" */
		if(  !Utils.isEmpty( beanA.getCoverId() )  ){
 			beanB.getId().setCoverId( beanA.getCoverId() ); 
 		}

 		/* Mapping: "commission" -> "commission" */
		if(  !Utils.isEmpty( beanA.getCommission() )  ){
 			beanB.setCommission( beanA.getCommission() ); 
 		}

 		/* Mapping: "coverSiAmt" -> "coverSiAmt" */
		if(  !Utils.isEmpty( beanA.getCoverSiAmt() )  ){
 			beanB.setCoverSiAmt( beanA.getCoverSiAmt() ); 
 		}

 		/* Mapping: "coverPrmAmt" -> "coverPrmAmt" */
		if(  !Utils.isEmpty( beanA.getCoverPrmAmt() )  ){
 			beanB.setCoverPrmAmt( beanA.getCoverPrmAmt() ); 
 		}

 		/* Mapping: "valStartDate" -> "valStartDate" */
		if(  !Utils.isEmpty( beanA.getValStartDate() )  ){
 			beanB.setValStartDate( beanA.getValStartDate() ); 
 		}

 		/* Mapping: "valExpDate" -> "valExpDate" */
		if(  !Utils.isEmpty( beanA.getValExpDate() )  ){
 			beanB.setValExpDate( beanA.getValExpDate() ); 
 		}

 		/* Mapping: "status" -> "status" */
		if(  !Utils.isEmpty( beanA.getStatus() )  ){
 			beanB.setStatus( beanA.getStatus() ); 
 		}

 		/* Mapping: "polQuoFlag" -> "polQuoFlag" */
		if(  !Utils.isEmpty( beanA.getPolQuoFlag() )  ){
 			beanB.setPolQuoFlag( beanA.getPolQuoFlag() ); 
 		}

   
		return dest;
	}	  

	/**
	 * This method initialises all the complex objects and collection instances we need for mapping.
	 */
	private static com.rsaame.pas.dao.model.VTrnPasPremiumSummary initializeDeepVO( com.rsaame.pas.vo.app.PremiumSummaryVO beanA, com.rsaame.pas.dao.model.VTrnPasPremiumSummary beanB ){
  		BeanUtils.initializeBeanField( "id", beanB );
                                		return beanB;
	}
}
