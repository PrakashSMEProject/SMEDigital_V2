       /*
 * THIS IS A GENERATED CLASS. THIS CLASS SHOULD NOT BE EDITED.
 */
package com.rsaame.pas.pojo.mapper;

import com.mindtree.ruc.cmn.beanmap.BaseBeanToBeanMapper;
import com.mindtree.ruc.cmn.beanmap.BeanMapperFactory;
import com.mindtree.ruc.cmn.beanmap.ConverterFactory;
import com.mindtree.ruc.cmn.log.Logger;
import com.mindtree.ruc.cmn.utils.BeanUtils;
import com.mindtree.ruc.cmn.utils.CopyUtils;
import com.mindtree.ruc.cmn.utils.Utils;

/**
 * Mapper class for:<ol>
 * <li>com.rsaame.pas.dao.model.TTrnPolicyQuo</li>
 * <li>com.rsaame.pas.vo.svc.TTrnPolicyVOHolder</li>
 * </ol>
 * Get an instance of this class by making a call to <code>BeanMapperFactory.getMapperInstance( TTrnPolicyVOHolderToTTrnPolicyReverse.class )</code>.
 */
public class TTrnPolicyVOHolderToTTrnPolicyReverse extends BaseBeanToBeanMapper<com.rsaame.pas.dao.model.TTrnPolicyQuo, com.rsaame.pas.vo.svc.TTrnPolicyVOHolder>{
	private final Logger log = Logger.getLogger( this.getClass() );	

	public TTrnPolicyVOHolderToTTrnPolicyReverse(){
		super();
	}

	public TTrnPolicyVOHolderToTTrnPolicyReverse( com.rsaame.pas.dao.model.TTrnPolicyQuo src, com.rsaame.pas.vo.svc.TTrnPolicyVOHolder dest ){
		super( src, dest );
	}

	/**
	 * This method maps the contents of the source bean to a destination bean.
	 * 
	 * @param src
	 * @param dest
	 */
	@Override
	public com.rsaame.pas.vo.svc.TTrnPolicyVOHolder mapBean(){
		
		/* Simply return if the source is null. */
		if( Utils.isEmpty( src ) ){
			return null;
		}
		
		/* If the destination is null, instantiate it. */
		if( Utils.isEmpty( dest ) ){
			dest = (com.rsaame.pas.vo.svc.TTrnPolicyVOHolder) Utils.newInstance( "com.rsaame.pas.vo.svc.TTrnPolicyVOHolder" );
		}
		
		/* Cast the destination bean to a bean of type of BeanA */	
		com.rsaame.pas.dao.model.TTrnPolicyQuo beanA = src;
			
		/* Cast the destination bean to a bean of type of BeanB */
		com.rsaame.pas.vo.svc.TTrnPolicyVOHolder beanB = dest;
			
  		/* Initialize any deepVO inside the destination bean. This is to avoid null pointers at runtime whenever a deep field is being mapped in the destination bean. */
		beanB = initializeDeepVO(beanA, beanB);
		
		/* A common variable that will be used to store the number of items in the case of multi-mappings. */
		int noOfItems = 0;
		
		/* Mapping: "id.polPolicyId" -> "polPolicyId" */
		if(  !Utils.isEmpty( beanA.getId() ) && !Utils.isEmpty( beanA.getId().getPolPolicyId() )  ){
 			beanB.setPolPolicyId( beanA.getId().getPolPolicyId() ); 
 		}

 		/* Mapping: "id.polEndtId" -> "polEndtId" */
		if(  !Utils.isEmpty( beanA.getId() ) && !Utils.isEmpty( beanA.getId().getPolEndtId() )  ){
 			beanB.setPolEndtId( beanA.getId().getPolEndtId() ); 
 		}

 		/* Mapping: "polLocationCode" -> "polLocationCode" */
		if(  !Utils.isEmpty( beanA.getPolLocationCode() )  ){
 			beanB.setPolLocationCode( beanA.getPolLocationCode() ); 
 		}

 		/* Mapping: "polClassCode" -> "polClassCode" */
		if(  !Utils.isEmpty( beanA.getPolClassCode() )  ){
 			beanB.setPolClassCode( beanA.getPolClassCode() ); 
 		}

 		/* Mapping: "polDocumentCode" -> "polDocumentCode" */
		if(  !Utils.isEmpty( beanA.getPolDocumentCode() )  ){
 			beanB.setPolDocumentCode( beanA.getPolDocumentCode() ); 
 		}

 		/* Mapping: "polPolicyNo" -> "polPolicyNo" */
		if(  !Utils.isEmpty( beanA.getPolPolicyNo() )  ){
 			beanB.setPolPolicyNo( beanA.getPolPolicyNo() ); 
 		}

 		/* Mapping: "polPolicyYear" -> "polPolicyYear" */
		if(  !Utils.isEmpty( beanA.getPolPolicyYear() )  ){
 			beanB.setPolPolicyYear( beanA.getPolPolicyYear() ); 
 		}

 		/* Mapping: "polRefPolicyNo" -> "polRefPolicyNo" */
		if(  !Utils.isEmpty( beanA.getPolRefPolicyNo() )  ){
 			beanB.setPolRefPolicyNo( beanA.getPolRefPolicyNo() ); 
 		}

 		/* Mapping: "polRefPolicyYear" -> "polRefPolicyYear" */
		if(  !Utils.isEmpty( beanA.getPolRefPolicyYear() )  ){
 			beanB.setPolRefPolicyYear( beanA.getPolRefPolicyYear() ); 
 		}

 		/* Mapping: "polPolicyType" -> "polPolicyType" */
		if(  !Utils.isEmpty( beanA.getPolPolicyType() )  ){
 			beanB.setPolPolicyType( beanA.getPolPolicyType() ); 
 		}

 		/* Mapping: "polProposalNo" -> "polProposalNo" */
		if(  !Utils.isEmpty( beanA.getPolProposalNo() )  ){
 			beanB.setPolProposalNo( beanA.getPolProposalNo() ); 
 		}

 		/* Mapping: "polProposalDate" -> "polProposalDate" */
		if(  !Utils.isEmpty( beanA.getPolProposalDate() )  ){
 			beanB.setPolProposalDate( beanA.getPolProposalDate() ); 
 		}

 		/* Mapping: "polIssueDate" -> "polIssueDate" */
		if(  !Utils.isEmpty( beanA.getPolIssueDate() )  ){
 			beanB.setPolIssueDate( beanA.getPolIssueDate() ); 
 		}

 		/* Mapping: "polExpiryDate" -> "polExpiryDate" */
		if(  !Utils.isEmpty( beanA.getPolExpiryDate() )  ){
 			beanB.setPolExpiryDate( beanA.getPolExpiryDate() ); 
 		}

 		/* Mapping: "polSourceOfBusiness" -> "polSourceOfBusiness" */
		if(  !Utils.isEmpty( beanA.getPolSourceOfBusiness() )  ){
 			beanB.setPolSourceOfBusiness( beanA.getPolSourceOfBusiness() ); 
 		}

 		/* Mapping: "polAgentId" -> "polAgentId" */
		if(  !Utils.isEmpty( beanA.getPolAgentId() )  ){
 			beanB.setPolAgentId( beanA.getPolAgentId() ); 
 		}

 		/* Mapping: "polCommisionId" -> "polCommisionId" */
		if(  !Utils.isEmpty( beanA.getPolCommisionId() )  ){
 			beanB.setPolCommisionId( beanA.getPolCommisionId() ); 
 		}

 		/* Mapping: "polCoinsuranceIndicator" -> "polCoinsuranceIndicator" */
		if(  !Utils.isEmpty( beanA.isPolCoinsuranceIndicator() )  ){
 			beanB.setPolCoinsuranceIndicator( beanA.isPolCoinsuranceIndicator() ); 
 		}

 		/* Mapping: "polCoinsuranceType" -> "polCoinsuranceType" */
		if(  !Utils.isEmpty( beanA.isPolCoinsuranceType() )  ){
 			beanB.setPolCoinsuranceType( beanA.isPolCoinsuranceType() ); 
 		}

 		/* Mapping: "polCoverNoteNo" -> "polCoverNoteNo" */
		if(  !Utils.isEmpty( beanA.getPolCoverNoteNo() )  ){
 			beanB.setPolCoverNoteNo( beanA.getPolCoverNoteNo() ); 
 		}

 		/* Mapping: "polCoverNoteDate" -> "polCoverNoteDate" */
		if(  !Utils.isEmpty( beanA.getPolCoverNoteDate() )  ){
 			beanB.setPolCoverNoteDate( beanA.getPolCoverNoteDate() ); 
 		}

 		/* Mapping: "polPremium" -> "polPremium" */
		if(  !Utils.isEmpty( beanA.getPolPremium() )  ){
 			beanB.setPolPremium( beanA.getPolPremium() ); 
 		}

 		/* Mapping: "polGovernmentTax" -> "polGovernmentTax" */
		if(  !Utils.isEmpty( beanA.getPolGovernmentTax() )  ){
 			beanB.setPolGovernmentTax( beanA.getPolGovernmentTax() ); 
 		}

 		/* Mapping: "polPolicyFees" -> "polPolicyFees" */
		if(  !Utils.isEmpty( beanA.getPolPolicyFees() )  ){
 			beanB.setPolPolicyFees( beanA.getPolPolicyFees() ); 
 		}

 		/* Mapping: "polCurrencyCode" -> "polCurrencyCode" */
		if(  !Utils.isEmpty( beanA.getPolCurrencyCode() )  ){
 			beanB.setPolCurrencyCode( beanA.getPolCurrencyCode() ); 
 		}

 		/* Mapping: "polExchangeRate" -> "polExchangeRate" */
		if(  !Utils.isEmpty( beanA.getPolExchangeRate() )  ){
 			beanB.setPolExchangeRate( beanA.getPolExchangeRate() ); 
 		}

 		/* Mapping: "polProcessedDate" -> "polProcessedDate" */
		if(  !Utils.isEmpty( beanA.getPolProcessedDate() )  ){
 			beanB.setPolProcessedDate( beanA.getPolProcessedDate() ); 
 		}

 		/* Mapping: "polStatus" -> "polStatus" */
		if(  !Utils.isEmpty( beanA.getPolStatus() )  ){
 			beanB.setPolStatus( beanA.getPolStatus() ); 
 		}

 		/* Mapping: "polFinancialIntInd" -> "polFinancialIntInd" */
		if(  !Utils.isEmpty( beanA.getPolFinancialIntInd() )  ){
 			beanB.setPolFinancialIntInd( beanA.getPolFinancialIntInd() ); 
 		}

 		/* Mapping: "polApprovedBy" -> "polApprovedBy" */
		if(  !Utils.isEmpty( beanA.getPolApprovedBy() )  ){
 			beanB.setPolApprovedBy( beanA.getPolApprovedBy() ); 
 		}

 		/* Mapping: "polUserId" -> "polUserId" */
		if(  !Utils.isEmpty( beanA.getPolUserId() )  ){
 			beanB.setPolUserId( beanA.getPolUserId() ); 
 		}

 		/* Mapping: "polEffectiveDate" -> "polEffectiveDate" */
		if(  !Utils.isEmpty( beanA.getPolEffectiveDate() )  ){
 			beanB.setPolEffectiveDate( beanA.getPolEffectiveDate() ); 
 		}

 		/* Mapping: "polPrintDate" -> "polPrintDate" */
		if(  !Utils.isEmpty( beanA.getPolPrintDate() )  ){
 			beanB.setPolPrintDate( beanA.getPolPrintDate() ); 
 		}

 		/* Mapping: "polCutCode" -> "polCutCode" */
		if(  !Utils.isEmpty( beanA.getPolCutCode() )  ){
 			beanB.setPolCutCode( beanA.getPolCutCode() ); 
 		}

 		/* Mapping: "polSpecialType" -> "polSpecialType" */
		if(  !Utils.isEmpty( beanA.getPolSpecialType() )  ){
 			beanB.setPolSpecialType( beanA.getPolSpecialType() ); 
 		}

 		/* Mapping: "polCoverNoteHour" -> "polCoverNoteHour" */
		if(  !Utils.isEmpty( beanA.getPolCoverNoteHour() )  ){
 			beanB.setPolCoverNoteHour( beanA.getPolCoverNoteHour() ); 
 		}

 		/* Mapping: "polCoverNoteMin" -> "polCoverNoteMin" */
		if(  !Utils.isEmpty( beanA.getPolCoverNoteMin() )  ){
 			beanB.setPolCoverNoteMin( beanA.getPolCoverNoteMin() ); 
 		}

 		/* Mapping: "polIssueHour" -> "polIssueHour" */
		if(  !Utils.isEmpty( beanA.getPolIssueHour() )  ){
 			beanB.setPolIssueHour( beanA.getPolIssueHour() ); 
 		}

 		/* Mapping: "polIssueMin" -> "polIssueMin" */
		if(  !Utils.isEmpty( beanA.getPolIssueMin() )  ){
 			beanB.setPolIssueMin( beanA.getPolIssueMin() ); 
 		}

 		/* Mapping: "polEffectiveHour" -> "polEffectiveHour" */
		if(  !Utils.isEmpty( beanA.getPolEffectiveHour() )  ){
 			beanB.setPolEffectiveHour( beanA.getPolEffectiveHour() ); 
 		}

 		/* Mapping: "polEffectiveMin" -> "polEffectiveMin" */
		if(  !Utils.isEmpty( beanA.getPolEffectiveMin() )  ){
 			beanB.setPolEffectiveMin( beanA.getPolEffectiveMin() ); 
 		}

 		/* Mapping: "polExpiryHour" -> "polExpiryHour" */
		if(  !Utils.isEmpty( beanA.getPolExpiryHour() )  ){
 			beanB.setPolExpiryHour( beanA.getPolExpiryHour() ); 
 		}

 		/* Mapping: "polExpiryMin" -> "polExpiryMin" */
		if(  !Utils.isEmpty( beanA.getPolExpiryMin() )  ){
 			beanB.setPolExpiryMin( beanA.getPolExpiryMin() ); 
 		}

 		/* Mapping: "polCertificateNo" -> "polCertificateNo" */
		if(  !Utils.isEmpty( beanA.getPolCertificateNo() )  ){
 			beanB.setPolCertificateNo( beanA.getPolCertificateNo() ); 
 		}

 		/* Mapping: "polCertificateDate" -> "polCertificateDate" */
		if(  !Utils.isEmpty( beanA.getPolCertificateDate() )  ){
 			beanB.setPolCertificateDate( beanA.getPolCertificateDate() ); 
 		}

 		/* Mapping: "polValidityStartDate" -> "polValidityStartDate" */
		if(  !Utils.isEmpty( beanA.getPolValidityStartDate() )  ){
 			beanB.setPolValidityStartDate( beanA.getPolValidityStartDate() ); 
 		}

 		/* Mapping: "polEndtEffectiveDate" -> "polEndtEffectiveDate" */
		if(  !Utils.isEmpty( beanA.getPolEndtEffectiveDate() )  ){
 			beanB.setPolEndtEffectiveDate( beanA.getPolEndtEffectiveDate() ); 
 		}

 		/* Mapping: "polEndtExpiryDate" -> "polEndtExpiryDate" */
		if(  !Utils.isEmpty( beanA.getPolEndtExpiryDate() )  ){
 			beanB.setPolEndtExpiryDate( beanA.getPolEndtExpiryDate() ); 
 		}

 		/* Mapping: "polRatingType" -> "polRatingType" */
		if(  !Utils.isEmpty( beanA.getPolRatingType() )  ){
 			beanB.setPolRatingType( beanA.getPolRatingType() ); 
 		}

 		/* Mapping: "polCcgCode" -> "polCcgCode" */
		if(  !Utils.isEmpty( beanA.getPolCcgCode() )  ){
 			beanB.setPolCcgCode( beanA.getPolCcgCode() ); 
 		}

 		/* Mapping: "polEndtNo" -> "polEndtNo" */
		if(  !Utils.isEmpty( beanA.getPolEndtNo() )  ){
 			beanB.setPolEndtNo( beanA.getPolEndtNo() ); 
 		}

 		/* Mapping: "polCtyCode" -> "polCtyCode" */
		if(  !Utils.isEmpty( beanA.getPolCtyCode() )  ){
 			beanB.setPolCtyCode( beanA.getPolCtyCode() ); 
 		}

 		/* Mapping: "polRegCode" -> "polRegCode" */
		if(  !Utils.isEmpty( beanA.getPolRegCode() )  ){
 			beanB.setPolRegCode( beanA.getPolRegCode() ); 
 		}

 		/* Mapping: "polPolicyTerm" -> "polPolicyTerm" */
		if(  !Utils.isEmpty( beanA.getPolPolicyTerm() )  ){
 			beanB.setPolPolicyTerm( beanA.getPolPolicyTerm() ); 
 		}

 		/* Mapping: "polRefPolicyId" -> "polRefPolicyId" */
		if(  !Utils.isEmpty( beanA.getPolRefPolicyId() )  ){
 			beanB.setPolRefPolicyId( beanA.getPolRefPolicyId() ); 
 		}

 		/* Mapping: "polAccexecCode" -> "polAccexecCode" */
		if(  !Utils.isEmpty( beanA.getPolAccexecCode() )  ){
 			beanB.setPolAccexecCode( beanA.getPolAccexecCode() ); 
 		}

 		/* Mapping: "polInsuredId" -> "polInsuredId" */
		if(  !Utils.isEmpty( beanA.getPolInsuredId() )  ){
 			beanB.setPolInsuredId( beanA.getPolInsuredId() ); 
 		}

 		/* Mapping: "polQuotationNo" -> "polQuotationNo" */
		if(  !Utils.isEmpty( beanA.getPolQuotationNo() )  ){
 			beanB.setPolQuotationNo( beanA.getPolQuotationNo() ); 
 		}

 		/* Mapping: "polBrCode" -> "polBrCode" */
		if(  !Utils.isEmpty( beanA.getPolBrCode() )  ){
 			beanB.setPolBrCode( beanA.getPolBrCode() ); 
 		}

 		/* Mapping: "polDctCode" -> "polDctCode" */
		if(  !Utils.isEmpty( beanA.getPolDctCode() )  ){
 			beanB.setPolDctCode( beanA.getPolDctCode() ); 
 		}

 		/* Mapping: "polPreparedBy" -> "polPreparedBy" */
		if(  !Utils.isEmpty( beanA.getPolPreparedBy() )  ){
 			beanB.setPolPreparedBy( beanA.getPolPreparedBy() ); 
 		}

 		/* Mapping: "polPreparedDt" -> "polPreparedDt" */
		if(  !Utils.isEmpty( beanA.getPolPreparedDt() )  ){
 			beanB.setPolPreparedDt( beanA.getPolPreparedDt() ); 
 		}

 		/* Mapping: "polModifiedBy" -> "polModifiedBy" */
		if(  !Utils.isEmpty( beanA.getPolModifiedBy() )  ){
 			beanB.setPolModifiedBy( beanA.getPolModifiedBy() ); 
 		}

 		/* Mapping: "polPostedDate" -> "polPostedDate" */
		if(  !Utils.isEmpty( beanA.getPolPostedDate() )  ){
 			beanB.setPolPostedDate( beanA.getPolPostedDate() ); 
 		}

 		/* Mapping: "polRefreshDate" -> "polRefreshDate" */
		if(  !Utils.isEmpty( beanA.getPolRefreshDate() )  ){
 			beanB.setPolRefreshDate( beanA.getPolRefreshDate() ); 
 		}

 		/* Mapping: "polConcPolicyNo" -> "polConcPolicyNo" */
		if(  !Utils.isEmpty( beanA.getPolConcPolicyNo() )  ){
 			beanB.setPolConcPolicyNo( beanA.getPolConcPolicyNo() ); 
 		}

 		/* Mapping: "polSocCode" -> "polSocCode" */
		if(  !Utils.isEmpty( beanA.getPolSocCode() )  ){
 			beanB.setPolSocCode( beanA.getPolSocCode() ); 
 		}

 		/* Mapping: "polTarCode" -> "polTarCode" */
		if(  !Utils.isEmpty( beanA.getPolTarCode() )  ){
 			beanB.setPolTarCode( beanA.getPolTarCode() ); 
 		}

 		/* Mapping: "polRenTermTxt" -> "polRenTermTxt" */
		if(  !Utils.isEmpty( beanA.getPolRenTermTxt() )  ){
 			beanB.setPolRenTermTxt( beanA.getPolRenTermTxt() ); 
 		}

 		/* Mapping: "polOtherInsurerCode" -> "polOtherInsurerCode" */
		if(  !Utils.isEmpty( beanA.getPolOtherInsurerCode() )  ){
 			beanB.setPolOtherInsurerCode( beanA.getPolOtherInsurerCode() ); 
 		}

 		/* Mapping: "polBusinessType" -> "polBusinessType" */
		if(  !Utils.isEmpty( beanA.getPolBusinessType() )  ){
 			beanB.setPolBusinessType( beanA.getPolBusinessType() ); 
 		}

 		/* Mapping: "polApprovalDate" -> "polApprovalDate" */
		if(  !Utils.isEmpty( beanA.getPolApprovalDate() )  ){
 			beanB.setPolApprovalDate( beanA.getPolApprovalDate() ); 
 		}

 		/* Mapping: "polSuspendTransaction" -> "polSuspendTransaction" */
		if(  !Utils.isEmpty( beanA.getPolSuspendTransaction() )  ){
 			beanB.setPolSuspendTransaction( beanA.getPolSuspendTransaction() ); 
 		}

 		/* Mapping: "polInsuredCode" -> "polInsuredCode" */
		if(  !Utils.isEmpty( beanA.getPolInsuredCode() )  ){
 			beanB.setPolInsuredCode( beanA.getPolInsuredCode() ); 
 		}

 		/* Mapping: "polDistributionChnl" -> "polDistributionChnl" */
		if(  !Utils.isEmpty( beanA.getPolDistributionChnl() )  ){
 			beanB.setPolDistributionChnl( beanA.getPolDistributionChnl() ); 
 		}

 		/* Mapping: "polRenewalCount" -> "polRenewalCount" */
		if(  !Utils.isEmpty( beanA.getPolRenewalCount() )  ){
 			beanB.setPolRenewalCount( beanA.getPolRenewalCount() ); 
 		}

 		/* Mapping: "polNajExpDate" -> "polNajExpDate" */
		if(  !Utils.isEmpty( beanA.getPolNajExpDate() )  ){
 			beanB.setPolNajExpDate( beanA.getPolNajExpDate() ); 
 		}

 		/* Mapping: "polReasonCode" -> "polReasonCode" */
		if(  !Utils.isEmpty( beanA.getPolReasonCode() )  ){
 			beanB.setPolReasonCode( beanA.getPolReasonCode() ); 
 		}

 		/* Mapping: "polApprovedInd" -> "polApprovedInd" */
		if(  !Utils.isEmpty( beanA.getPolApprovedInd() )  ){
 			beanB.setPolApprovedInd( beanA.getPolApprovedInd() ); 
 		}

 		/* Mapping: "polLinkingId" -> "polLinkingId" */
		if(  !Utils.isEmpty( beanA.getPolLinkingId() )  ){
 			beanB.setPolLinkingId( beanA.getPolLinkingId() ); 
 		}

 		/* Mapping: "polRenClmCount" -> "polRenClmCount" */
		if(  !Utils.isEmpty( beanA.getPolRenClmCount() )  ){
 			beanB.setPolRenClmCount( beanA.getPolRenClmCount() ); 
 		}

 		/* Mapping: "polSbsInd" -> "polSbsInd" */
		if(  !Utils.isEmpty( beanA.getPolSbsInd() )  ){
 			beanB.setPolSbsInd( beanA.getPolSbsInd() ); 
 		}

 		/* Mapping: "polProcLocCode" -> "polProcLocCode" */
		if(  !Utils.isEmpty( beanA.getPolProcLocCode() )  ){
 			beanB.setPolProcLocCode( beanA.getPolProcLocCode() ); 
 		}

 		/* Mapping: "polPepCode" -> "polPepCode" */
		if(  !Utils.isEmpty( beanA.getPolPepCode() )  ){
 			beanB.setPolPepCode( beanA.getPolPepCode() ); 
 		}

 		/* Mapping: "polPymntDueDate" -> "polPymntDueDate" */
		if(  !Utils.isEmpty( beanA.getPolPymntDueDate() )  ){
 			beanB.setPolPymntDueDate( beanA.getPolPymntDueDate() ); 
 		}

 		/* Mapping: "polPpwDate" -> "polPpwDate" */
		if(  !Utils.isEmpty( beanA.getPolPpwDate() )  ){
 			beanB.setPolPpwDate( beanA.getPolPpwDate() ); 
 		}

 		/* Mapping: "polFgnCurrPrm" -> "polFgnCurrPrm" */
		if(  !Utils.isEmpty( beanA.getPolFgnCurrPrm() )  ){
 			beanB.setPolFgnCurrPrm( beanA.getPolFgnCurrPrm() ); 
 		}

 		/* Mapping: "polAutoCession" -> "polAutoCession" */
		if(  !Utils.isEmpty( beanA.getPolAutoCession() )  ){
 			beanB.setPolAutoCession( beanA.getPolAutoCession() ); 
 		}

   
		return dest;
	}	  

	/**
	 * This method initialises all the complex objects and collection instances we need for mapping.
	 */
	private static com.rsaame.pas.vo.svc.TTrnPolicyVOHolder initializeDeepVO( com.rsaame.pas.dao.model.TTrnPolicyQuo beanA, com.rsaame.pas.vo.svc.TTrnPolicyVOHolder beanB ){
                                                                                                                                                                                 		return beanB;
	}
}
