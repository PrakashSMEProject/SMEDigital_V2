/*
 * THIS IS A GENERATED CLASS. THIS CLASS SHOULD NOT BE EDITED.
 */
package gen.b2b;

import com.mindtree.ruc.cmn.beanmap.BaseBeanToBeanMapper;
import com.mindtree.ruc.cmn.beanmap.BeanMapperFactory;
import com.mindtree.ruc.cmn.beanmap.ConverterFactory;
import com.mindtree.ruc.cmn.log.Logger;
import com.mindtree.ruc.cmn.utils.BeanUtils;
import com.mindtree.ruc.cmn.utils.CopyUtils;
import com.mindtree.ruc.cmn.utils.Utils;

/**
 * Mapper class for:<ol>
 * <li>com.mindtree.devtools.b2b.sample.SourceBean</li>
 * <li>com.mindtree.devtools.b2b.sample.DestBean</li>
 * </ol>
 * Get an instance of this class by making a call to <code>BeanMapperFactory.getMapperInstance( SourceDestBeanMapper.class )</code>.
 */
public class SourceDestBeanMapper extends BaseBeanToBeanMapper<com.mindtree.devtools.b2b.sample.SourceBean, com.mindtree.devtools.b2b.sample.DestBean>{
	private final Logger log = Logger.getLogger( this.getClass() );	

	public SourceDestBeanMapper(){
		super();
	}

	public SourceDestBeanMapper( com.mindtree.devtools.b2b.sample.SourceBean src, com.mindtree.devtools.b2b.sample.DestBean dest ){
		super( src, dest );
	}

	/**
	 * This method maps the contents of the source bean to a destination bean.
	 * 
	 * @param src
	 * @param dest
	 */
	@Override
	public com.mindtree.devtools.b2b.sample.DestBean mapBean(){
		
		/* Simply return if the source is null. */
		if( Utils.isEmpty( src ) ){
			return null;
		}
		
		/* If the destination is null, instantiate it. */
		if( Utils.isEmpty( dest ) ){
			dest = (com.mindtree.devtools.b2b.sample.DestBean) Utils.newInstance( "com.mindtree.devtools.b2b.sample.DestBean" );
		}
		
		/* Cast the destination bean to a bean of type of BeanA */	
		com.mindtree.devtools.b2b.sample.SourceBean beanA = src;
			
		/* Cast the destination bean to a bean of type of BeanB */
		com.mindtree.devtools.b2b.sample.DestBean beanB = dest;
			
  		/* Initialize any deepVO inside the destination bean. This is to avoid null pointers at runtime whenever a deep field is being mapped in the destination bean. */
		beanB = initializeDeepVO(beanB);
		
		/* The following represent the bean copy methods */		
				{
			//This is a mapping for attribute which is a collection of complex data type or simple data type where the source and destination collection are of same type
			BaseBeanToBeanMapper mapper = BeanMapperFactory.getMapperInstanceForRef( com.Constant.CONST_BEANATOBEANCMAPPER, CopyUtils.asList( beanA.getBeanAList() ).get( 0 ) );
			beanB.setBeanCList( CopyUtils.copy( beanA.getBeanAList(), beanB.getBeanCList(), mapper.getClass() ));
		}
 		{
			//This is a mapping for attribute which is a collection of complex data type or simple data type where the source and destination collection are of same type
			BaseBeanToBeanMapper mapper = BeanMapperFactory.getMapperInstanceForRef( com.Constant.CONST_BEANATOBEANCMAPPER, CopyUtils.asList( beanA.getBeanAList() ).get( 0 ) );
			beanB.setBeanCArray( CopyUtils.copy( beanA.getBeanAList(), beanB.getBeanCArray(), mapper.getClass() ));
		}
 		{
			//This is a mapping for attribute which is a collection of complex data type or simple data type where the source and destination collection are of same type
			BaseBeanToBeanMapper mapper = BeanMapperFactory.getMapperInstanceForRef( com.Constant.CONST_BEANATOBEANCMAPPER, CopyUtils.asList( beanA.getBeanAList() ).get( 0 ) );
			beanB.setBeanCSet( CopyUtils.copy( beanA.getBeanAList(), beanB.getBeanCSet(), mapper.getClass() ));
		}
 		{
			//This is a mapping for attribute which is a collection of complex data type or simple data type where the source and destination collection are of same type
			BaseBeanToBeanMapper mapper = BeanMapperFactory.getMapperInstanceForRef( com.Constant.CONST_BEANATOBEANCMAPPER, CopyUtils.asList( beanA.getBeanAArray() ).get( 0 ) );
			beanB.setBeanCArray( CopyUtils.copy( beanA.getBeanAArray(), beanB.getBeanCArray(), mapper.getClass() ));
		}
 		{
			//This is a mapping for attribute which is a collection of complex data type or simple data type where the source and destination collection are of same type
			BaseBeanToBeanMapper mapper = BeanMapperFactory.getMapperInstanceForRef( com.Constant.CONST_BEANATOBEANCMAPPER, CopyUtils.asList( beanA.getBeanAArray() ).get( 0 ) );
			beanB.setBeanCList( CopyUtils.copy( beanA.getBeanAArray(), beanB.getBeanCList(), mapper.getClass() ));
		}
 		{
			//This is a mapping for attribute which is a collection of complex data type or simple data type where the source and destination collection are of same type
			BaseBeanToBeanMapper mapper = BeanMapperFactory.getMapperInstanceForRef( com.Constant.CONST_BEANATOBEANCMAPPER, CopyUtils.asList( beanA.getBeanAArray() ).get( 0 ) );
			beanB.setBeanCSet( CopyUtils.copy( beanA.getBeanAArray(), beanB.getBeanCSet(), mapper.getClass() ));
		}
 		{
			//This is a mapping for attribute which is a collection of complex data type or simple data type where the source and destination collection are of same type
			BaseBeanToBeanMapper mapper = BeanMapperFactory.getMapperInstanceForRef( com.Constant.CONST_BEANATOBEANCMAPPER, CopyUtils.asList( beanA.getBeanASet() ).get( 0 ) );
			beanB.setBeanCSet( CopyUtils.copy( beanA.getBeanASet(), beanB.getBeanCSet(), mapper.getClass() ));
		}
 		{
			//This is a mapping for attribute which is a collection of complex data type or simple data type where the source and destination collection are of same type
			BaseBeanToBeanMapper mapper = BeanMapperFactory.getMapperInstanceForRef( com.Constant.CONST_BEANATOBEANCMAPPER, CopyUtils.asList( beanA.getBeanASet() ).get( 0 ) );
			beanB.setBeanCList( CopyUtils.copy( beanA.getBeanASet(), beanB.getBeanCList(), mapper.getClass() ));
		}
 		{
			//This is a mapping for attribute which is a collection of complex data type or simple data type where the source and destination collection are of same type
			BaseBeanToBeanMapper mapper = BeanMapperFactory.getMapperInstanceForRef( com.Constant.CONST_BEANATOBEANCMAPPER, CopyUtils.asList( beanA.getBeanASet() ).get( 0 ) );
			beanB.setBeanCArray( CopyUtils.copy( beanA.getBeanASet(), beanB.getBeanCArray(), mapper.getClass() ));
		}
   
		return dest;
	}	  

	/**
	 * This method initializes the deepVOs inside the destination bean.
	 * 
	 * @param beanB
	 * @return beanB
	 */
	private static com.mindtree.devtools.b2b.sample.DestBean initializeDeepVO( com.mindtree.devtools.b2b.sample.DestBean beanB ){	
		return beanB;
	}
}
