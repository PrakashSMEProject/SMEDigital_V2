package com.rsaame.pas.vo.bus;

/**
 * This enum has constants for the various risk grouping levels. A risk grouping level means
 * the level at which details are captured for a section. In some cases, the risk details are
 * captured at a location-level and in some others, it is directly at the section level.
 */
public enum RiskGroupingLevel {
	SECTION,
	LOCATION;
}
